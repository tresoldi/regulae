# Parity checks that are expected to differ, one "corpus<TAB>part<TAB>reason"
# per line. scripts/parity.sh still prints these diffs but does not fail on
# them. Anything not listed here is a real regression.
#
# Both entries below are floating-point tie artifacts, not behavioural
# differences: the values the two implementations print are identical, and only
# the choice between equally-costed results differs.
real_romance_4lect.tsv	summary	DP tie: hair-035 french>italian has two alignments of identical cost (-2.325512486776); the winner depends on the order rounding error accumulates in the DP cost array. It shifts which l the french v links to, moving three classes between the latin ll / spanish ʎ groupings.
real_romance_4lect.tsv	pairwise	Same DP tie as above, seen in the per-pair tables.
real_romance_4lect.tsv	outliers	Same DP tie as above, seen in the per-cognate costs.
real_tone_chinese.tsv	outliers	Row order among cognates whose cost and z-score are equal to printed precision; the underlying z-scores differ only in their last bits, so the two sorts interleave the tied rows differently.
